name: first_feature
description: Feature that uses packages and has been used in test applications
resolution: workspace
publish_to: none
version: 0.0.1+1

environment:
  sdk: ^3.9.0
  flutter: ^3.35.0

dependencies:
  core: ^0.0.1+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0
